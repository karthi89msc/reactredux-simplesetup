const inititalState = [];
export const SampleReducer = (state, action) => {
    switch (action.type) {
        case 'INCREMENT':
            console.log('here');
    }
    return { name: 'Karthik1234' };
}